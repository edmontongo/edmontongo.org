machine:
  environment:
    CIRCLE_BUILD_DIR: $HOME/$CIRCLE_PROJECT_REPONAME

dependencies:
  pre:
    - bash ./install-sassc.sh
    - bash ./install-hugo.sh
  cache_directories:
    - hugo_0.12_linux_amd64

test:
  override:
    - bin/sassc sass/all.sass static/assets/all.css --style compressed
    - hugo -v
